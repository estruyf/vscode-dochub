export const EXTENSION_NAME = "dochub";

export const COMMAND = {
  addDocs: `${EXTENSION_NAME}.addDocs`,
  openDocs: `${EXTENSION_NAME}.openDocs`,
  openInBrowser: `${EXTENSION_NAME}.openInBrowser`,
  // Config
  openConfig: `${EXTENSION_NAME}.openConfig`,
  // Panel
  collapseAll: `${EXTENSION_NAME}.collapseAll`,
  // Output
  showOutputChannel: `${EXTENSION_NAME}.showOutputChannel`,
};
